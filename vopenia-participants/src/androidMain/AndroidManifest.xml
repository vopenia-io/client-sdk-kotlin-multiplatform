<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- register features that are used but can be optional (Publisher vs Subscriber) -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.microphone"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.autofocus"
        android:required="false" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <!-- Proximity wake lock: blanks the screen when the phone is at the ear while
         routed to the earpiece (LocalParticipant.setAudioRoute). -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application>

        <provider
            android:name="io.vopenia.livekit.HoldApplicationContext"
            android:authorities="io.vopenia.livekit.HoldApplicationContext"
            android:enabled="true"
            android:exported="false" />
    </application>
</manifest>